using UnityEngine;
using System.Collections;

public class StartGameManager : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Debug.Log ("MainMenuScene userId: " + UserInfo.id);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
